// Implement a function called countUniqueValues, which accepts a sorted array, and counts the unique values in the array.
// There can be negative number in the array, but it will always be sorted. 

type Players = {

playerName: string | number; 

// function countUniqueJerseys (arr: number[]): void {

// // while left < right, let uniqueCounterVariable = 0
// // for let uniqNum of arr.left {arr[left] = (uniqueCounterVariable[])}

//   let left = 0;
//   let right = arr.length -1;

// }

}